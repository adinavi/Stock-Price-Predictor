Build the Project: Navigate to the project directory and create a build folder:

sh
Copy code
mkdir build
cd build
cmake ..
make
Run the Program: After building the project, you can run the executable:

sh
Copy code
./StockPricePredictor


